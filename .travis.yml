language: bash

before_script:
    - curl -fsSL https://git.io/shellspec|sh -s -- --yes
    - export PATH=$PATH:$HOME/.local/bin/
    - mkdir -pv $PATH:$HOME/.bkit/etc
    - mkdir -pv $PATH:$HOME/.bkit/var

script:
    - ./devtools/test_spec.sh