language: bash

before_script:
    - curl -fsSL https://git.io/shellspec|sh -s -- --yes
    - export PATH=$PATH:$HOME/.local/bin/
    - mkdir -pv /home/travis/.bkit/etc
    - mkdir -pv /home/travis/.bkit/var

script:
    - ./devtools/test_spec.sh